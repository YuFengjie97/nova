interface NavItem {
  title: string
  link: string
  bgPos?: Array<string>
}

interface Position {
  x: number
  y: number
}

interface Vector {
  vx: number
  vy: number
}

type Point = [number, number]
